# Deprecated

Use **`.agents/templates/plan-template.md`** instead.

The SDD agent writes **`plan.md`** in `.agents-state/current/`. The filename `sdd.md` is legacy — read `plan.md` first; use `sdd.md` only if `plan.md` is missing.
